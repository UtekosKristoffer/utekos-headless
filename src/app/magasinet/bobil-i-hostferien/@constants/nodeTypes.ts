import ChecklistNode from '@/app/magasinet/bobil-i-hostferien/CheckListNode'

export const nodeTypes = {
  checklist: ChecklistNode
  // Du kan beholde 'custom: CustomNode' her hvis du vil gjenbruke den andre steder
}
